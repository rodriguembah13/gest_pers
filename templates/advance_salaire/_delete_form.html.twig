<form method="post" action="{{ path('advance_salaire_delete', {'id': advance_salaire.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ advance_salaire.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
