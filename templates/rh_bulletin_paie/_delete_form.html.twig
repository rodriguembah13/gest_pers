<form method="post" action="{{ path('rh_bulletin_paie_delete', {'id': rh_bulletin_paie.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ rh_bulletin_paie.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
