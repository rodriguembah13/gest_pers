{% extends 'base.html.twig' %}

{% block title %}TypeContrat{% endblock %}

{% block page_content %}
    <h1>TypeContrat</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_contrat.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ type_contrat.libelle }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ type_contrat.code }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('type_contrat_index') }}">back to list</a>

    <a href="{{ path('type_contrat_edit', {'id': type_contrat.id}) }}">edit</a>

    {{ include('type_contrat/_delete_form.html.twig') }}
{% endblock %}
