{{ form_start(form) }}
<div class="col-md-6">
    {{ form_row(form.employe) }}
    {{ form_row(form.echance) }}
</div>
<div class="col-md-6">
    {{ form_row(form.montant) }}

</div>
    {{ form_widget(form) }}
{#<div class="pull-right">
    <button type="reset" class="btn btn-warning "><i class="fa fa-times"></i> {{ 'Cancel'|trans }}</button>
    <button type="submit" class="btn btn-primary "><i class="fa fa-check-square"></i> {{ button_label|default('Suivant') }}</button>
</div>#}
{{ form_end(form) }}
