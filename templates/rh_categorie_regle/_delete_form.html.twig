<form method="post" action="{{ path('rh_categorie_regle_delete', {'id': rh_categorie_regle.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ rh_categorie_regle.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
