{% extends 'base.html.twig' %}

{% block title %}Currency{% endblock %}

{% block body %}
    <h1>Currency</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ currency.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ currency.libelle }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ currency.code }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ currency.status ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('currency_index') }}">back to list</a>

    <a href="{{ path('currency_edit', {'id': currency.id}) }}">edit</a>

    {{ include('currency/_delete_form.html.twig') }}
{% endblock %}
