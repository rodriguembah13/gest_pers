{% extends 'base.html.twig' %}

{% block title %}Poste{% endblock %}
{% block page_title %} Poste {% endblock %}
{% block page_subtitle %} detail {% endblock %}
{% block page_content %}

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ poste.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ poste.libelle }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ poste.code }}</td>
            </tr>
        </tbody>
    </table>
    <div class="row">


        <div class="col-md-9"> <a class="btn btn-warning" href="{{ path('poste_index') }}">back to list</a>

            <a class="btn btn-primary" href="{{ path('poste_edit', {'id': poste.id}) }}">edit</a></div>
   <div class="col-md-3"> {{ include('poste/_delete_form.html.twig') }}</div>
    </div>
{% endblock %}
