{% extends 'base.html.twig' %}

{% block title %}Holiday{% endblock %}
{% block page_title %} Holiday {% endblock %}
{% block page_subtitle %} Detail {% endblock %}
{% block page_content %}
    <table class="table contentimg">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ holiday.id }}</td>
            </tr>
            <tr>
                <th>Day</th>
                <td>{{ holiday.day ? holiday.day|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <div class="col-md-9"> <a class="btn btn-warning" href="{{ path('holiday_index') }}">back to list</a>

        <a class="btn btn-primary" href="{{ path('holiday_edit', {'id': holiday.id}) }}">edit</a></div>
    <div class="col-md-3"> {{ include('holiday/_delete_form.html.twig') }}</div>
{% endblock %}
