{% extends 'base.html.twig' %}

{% block title %}Edit WorkDay{% endblock %}

{% block body %}
    <h1>Edit WorkDay</h1>

    {{ include('work_day/_form.html.twig', {'button_label': 'Update'}) }}

    <div class="row">
        <div class="col-md-2"><a class="btn btn-warning"  href="{{ path('work_day_index') }}"><i class="fa fa-arrow-left"></i> back to list</a></div>
        {{ include('work_day/_delete_form.html.twig') }}
    </div>
{% endblock %}
