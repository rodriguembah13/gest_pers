{% extends 'base.html.twig' %}

{% block title %}Module index{% endblock %}
{% block page_title %} Module {% endblock %}
{% block page_subtitle %} list {% endblock %}
{% block page_content %}
    <div class="row">
        <div class="col-md-4">

        </div>
        <div class="col-md-4">
            <div class="input-group">
                <input class="form-control" id="Search" name="Search" ng-change="vm.getElevesSearch(vm.search)" ng-model="vm.search"
                       placeholder="recherche " type="text"/>
                <div class="input-group-btn">
                    <button class="btn btn-default" ng-click="vm.resetInput(vm.search)"><i
                                class="glyphicon glyphicon-refresh glyphicon-spin"></i></button>
                </div>
            </div>
        </div>
        <div class="col-md-3">
            <div class="btn-group">
                <a class="btn btn-info btn-sm" href="#" id="jqtable_emplist"><i class="fa fa-th-list"></i></a>
                <a  class="btn btn-default btn-sm" href="#" id="jqtable_empbox"><i class="fa fa-th"></i></a>
            </div>
            <a class="btn btn-primary btn-sm" href="{{ path('module_new') }}"><i class="fa fa-flash"></i>Create new</a>
            <a class="btn btn-success btn-sm pull-right" href="{{ path('module_new') }}"><i class="far fa-flash"></i>Importer</a>
        </div>
    </div>
    <br>
        {% for module in modules %}
            <div class="col-md-4">
                    {# ... infobox widget, solid background ... #}
                {% embed '@AdminLTE/Widgets/box-widget.html.twig' %}

                        {% block box_body %}
                            {{ module.libelle }}
                        {% endblock %}
                        {% block box_footer %}{{ module.active ? '<a href="#"><i class="fas fa-circle text-success"></i></a>' : '<i class="fas fa-circle text-danger"></i>' }}
                        {#<a class="btn btn-primary">activate</a>#}
                        {% endblock %}
                    {% endembed %}

            </div> {% endfor %}
           {# <tr>
                <td>{{ module.id }}</td>
                <td>{{ module.libelle }}</td>
                <td>{{ module.code }}</td>
                <td>{{ module.active ? 'Yes' : 'No' }}</td>
                <td>{{ module.dateEnd ? module.dateEnd|date('Y-m-d') : '' }}</td>
                <td>{{ module.passhash }}</td>
                <td>
                    <a href="{{ path('module_show', {'id': module.id}) }}">show</a>
                    <a href="{{ path('module_edit', {'id': module.id}) }}">edit</a>
                </td>
            </tr>#}

   {# <a href="{{ path('module_new') }}">Create new</a>#}
{% endblock %}
