{% extends 'base.html.twig' %}

{% block title %}Rhlignereglepaie{% endblock %}

{% block body %}
    <h1>Rhlignereglepaie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ rhlignereglepaie.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ rhlignereglepaie.libelle }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ rhlignereglepaie.quantite }}</td>
            </tr>
            <tr>
                <th>Total</th>
                <td>{{ rhlignereglepaie.total }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('rhlignereglepaie_index') }}">back to list</a>

    <a href="{{ path('rhlignereglepaie_edit', {'id': rhlignereglepaie.id}) }}">edit</a>

    {{ include('rhlignereglepaie/_delete_form.html.twig') }}
{% endblock %}
