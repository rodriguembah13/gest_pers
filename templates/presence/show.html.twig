{% extends 'base.html.twig' %}

{% block title %}Presence{% endblock %}
{% block page_title %} Presence {% endblock %}
{% block page_subtitle %} Detail {% endblock %}
{% block page_content %}
    <table class="table contentimg">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ presence.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ presence.dateCreation ? presence.dateCreation|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>DateArrivee</th>
                <td>{{ presence.heureArrivee ? presence.heureArrivee|date('h:i A') : '' }}</td>
            </tr>
            <tr>
                <th>DateDepart</th>
                <td>{{ presence.heureDepart ? presence.heureDepart|date('h:i A') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <div class="col-md-9"> <a class="btn btn-warning" href="{{ path('presence_index') }}">back to list</a>

        <a class="btn btn-primary" href="{{ path('presence_edit', {'id': presence.id}) }}">edit</a></div>
    <div class="col-md-3"> {{ include('presence/_delete_form.html.twig') }}</div>
    </div>
{% endblock %}
