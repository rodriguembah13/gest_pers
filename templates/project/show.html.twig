{% extends 'base.html.twig' %}

{% block title %}Project{% endblock %}
{% block page_title %} Project {% endblock %}
{% block page_subtitle %} show {% endblock %}
{% block page_content %}
{#<div class="col-md-4">
<div class="card">
    <h3>{{ project.libelle}}</h3>
</div>
</div>
    <div class="col-md-8">

    </div>#}
    <table class="table">
        <tbody class="contentimg">
            <tr>
                <th>Id</th>
                <td>{{ project.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ project.libelle }}</td>
            </tr>
            <tr>
                <th>DateDebut</th>
                <td>{{ project.dateDebut ? project.dateDebut|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>DateFin</th>
                <td>{{ project.dateFin ? project.dateFin|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <div class="col-md-9"> <a class="btn btn-warning" href="{{ path('project_index') }}">back to list</a>

        <a class="btn btn-primary" href="{{ path('project_edit', {'id': project.id}) }}">edit</a></div>
    <div class="col-md-3"> {{ include('project/_delete_form.html.twig') }}</div>
{% endblock %}
