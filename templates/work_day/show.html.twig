{% extends 'base.html.twig' %}

{% block title %}WorkDay{% endblock %}

{% block body %}
    <h1>WorkDay</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ work_day.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ work_day.libelle }}</td>
            </tr>
            <tr>
                <th>HeureDebut</th>
                <td>{{ work_day.heureDebut ? work_day.heureDebut|date('H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>HeureFin</th>
                <td>{{ work_day.heureFin ? work_day.heureFin|date('H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>NombreHeure</th>
                <td>{{ work_day.nombreHeure }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('work_day_index') }}">back to list</a>

    <a href="{{ path('work_day_edit', {'id': work_day.id}) }}">edit</a>

    {{ include('work_day/_delete_form.html.twig') }}
{% endblock %}
